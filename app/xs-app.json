{
  "authenticationMethod": "route",
  "routes": [
    {
      "source": "^/(.*)$",
      "target": "$1",
      "destination": "srv-url",
      "authenticationType": "xsuaa",
      "csrfProtection": true
    }
  ]
}